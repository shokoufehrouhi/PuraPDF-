# مستند معماری و رودمپ اپ PDF Tools (نسخه ۲ — کراس‌پلتفرم)

## الزامات کلیدی این نسخه
- ران روی iOS و Android از **یک کدبیس**
- سرعت اجرا بالا (نزدیک به نیتیو)
- حجم نصب کم
- اتصال به Google Drive برای آپلود/دانلود فایل
- هر تغییر فقط یک‌بار نوشته بشه و برای هر دو استور بیلد بگیریم

---

## بخش ۱: تصمیم معماری — چرا Flutter

برای این پروژه سه گزینه اصلی برای کراس‌پلتفرم وجود داره: **Flutter**، **React Native**، و **Kotlin Multiplatform (KMP)**. مقایسه بر اساس نیازهای تو:

| معیار | Flutter | React Native | KMP |
|---|---|---|---|
| یک کدبیس برای UI هر دو پلتفرم | بله، کامل | بله، کامل | خیر (UI جدا نوشته می‌شه) |
| سرعت اجرا | نزدیک نیتیو (کامپایل به کد ماشین) | متوسط (پل JS-Native) | نیتیو کامل |
| حجم اپ پایه | ~۵-۷ مگابایت | ~۸-۱۲ مگابایت | خیلی کم |
| کار با فایل PDF/دوربین/فایل سیستم | پکیج‌های بالغ و زیاد | پکیج‌های خوب ولی گاهی نیاز به native module | نیاز به کد جدا هر پلتفرم |
| مناسب با Claude Code | خیلی خوب (یک زبان Dart، ساختار خطی) | خوب | پیچیده‌تر برای تولید خودکار کد |
| یادگیری/نگهداری | ساده‌تر (یک زبان) | ساده (JS/TS) | سخت‌تر (دو تا سه زبان) |

**نتیجه:** با توجه به این‌که پردازش PDF نیاز به کتابخانه‌های سنگین native داره و می‌خوای سرعت بالا + حجم کم + یک کدبیس واقعی داشته باشی، **Flutter** بهترین انتخابه. React Native هم گزینه قابل قبوله ولی برای عملیات سنگین PDF (فشرده‌سازی، رندر صفحات) کندتره چون از پل جاوااسکریپت رد می‌شه.

---

## بخش ۲: معماری فنی اپ

### لایه‌بندی (Clean Architecture ساده‌شده)
```
lib/
├── core/                  # ابزارهای مشترک: تم، ثابت‌ها، error handling
├── data/                  # منابع داده: local storage, Drive API, file system
│   ├── datasources/
│   └── repositories/
├── domain/                # منطق تجاری خالص (مستقل از UI و فریمورک)
│   ├── entities/
│   └── usecases/
├── presentation/          # UI و state management
│   ├── features/
│   │   ├── merge/
│   │   ├── split/
│   │   ├── compress/
│   │   ├── scan/
│   │   └── drive_sync/
│   └── shared_widgets/
└── main.dart
```

**چرا این ساختار؟**
- منطق تجاری (`domain`) از UI جدا هست → تست‌پذیر و قابل نگهداری
- هر فیچر پوشه مستقل داره → توسعه موازی و بدون تداخل راحت‌تره (مهم وقتی با Claude Code فاز به فاز جلو می‌ری)
- تغییر در یک فیچر، بقیه رو خراب نمی‌کنه

### مدیریت state
- پیشنهاد: **Riverpod** (سبک، تایپ‌سیف، دیباگ راحت) به‌جای Provider یا BLoC سنگین‌تر

### پردازش PDF — لوکال روی دستگاه
- تمام عملیات (merge/split/compress) روی خود گوشی انجام می‌شه، نه سرور
- مزیت: سرعت بالا (بدون تاخیر شبکه)، حریم خصوصی بهتر، هزینه سرور صفر
- پکیج پیشنهادی: `syncfusion_flutter_pdf` (رایگان تا حد مشخصی، قدرتمند) یا ترکیب `pdfrx` + `image` برای موارد سبک‌تر

### کوچک نگه داشتن حجم اپ
- استفاده از **App Bundle** (اندروید) و **App Thinning** (iOS) — استور خودش فقط منابع لازم برای هر دستگاه رو دانلود می‌کنه
- Lazy loading پکیج‌های سنگین (مثل OCR) فقط وقتی کاربر واقعاً استفاده می‌کنه
- فونت‌ها و آیکون‌ها بهینه (SVG به‌جای PNG هرجا ممکنه)
- Tree-shaking خودکار Flutter در بیلد release فعاله

### بالا نگه داشتن سرعت
- پردازش سنگین (فشرده‌سازی، رندر چند صفحه‌ای) در **Isolate جدا** اجرا بشه تا UI فریز نشه
- Lazy list rendering برای لیست فایل‌ها (فقط آیتم‌های دیده‌شده رندر بشن)
- کش کردن thumbnail صفحات PDF به‌جای رندر مجدد هر بار

---

## بخش ۳: اتصال به Google Drive

### روش پیاده‌سازی
- استفاده از پکیج `google_sign_in` برای احراز هویت
- استفاده از `googleapis` (پکیج رسمی Dart) برای فراخوانی Drive API v3
- Scope محدود به `drive.file` (اپ فقط به فایل‌های خودش دسترسی داره، نه کل درایو کاربر) — هم امن‌تره هم تایید گوگل راحت‌تر می‌گیره

### فیچرهای اتصال Drive
1. **ورود با گوگل** (یک بار، توکن ذخیره می‌مونه)
2. **آپلود** فایل PDF ساخته‌شده در اپ به یک پوشه مشخص در Drive (مثلاً `PDF Tools App`)
3. **دانلود/import** فایل PDF از Drive برای ویرایش در اپ
4. **همگام‌سازی وضعیت**: نمایش این‌که فایل local فقط هست، فقط در Drive هست، یا هر دو (sync شده)
5. (اختیاری فاز بعد) Auto-backup: هر فایل جدید خودکار به Drive آپلود بشه (قابل روشن/خاموش کردن)

### نکته فنی مهم
Google برای اپ‌هایی که به Drive API دسترسی دارن نیاز به **OAuth verification** داره (مخصوصاً اگه بخوای بعداً Scope رو گسترش بدی). با Scope محدود `drive.file` این پروسه ساده‌تره و معمولاً نیاز به بررسی دستی گوگل نداره.

---

## بخش ۴: یک‌بار نوشتن، بیلد برای هر دو استور

این دقیقاً مزیت اصلی Flutteره. جریان کار:

```
تغییر کد در lib/  (یک‌بار)
        │
        ├──> flutter build appbundle   → آپلود در Google Play Console
        │
        └──> flutter build ipa         → آپلود در App Store Connect (نیاز به Mac یا CI مثل Codemagic)
```

**نکته مهم درباره iOS:** برای بیلد نهایی iOS (فایل `.ipa`) نیاز به **Xcode** داری که فقط روی macOS نصب می‌شه. اگه مک نداری، راه‌حل‌ها:
- استفاده از سرویس CI/CD ابری مثل **Codemagic** یا **GitHub Actions با runner macOS** — کد رو push می‌کنی، خودش روی سرور مک بیلد می‌گیره و مستقیم به App Store Connect می‌فرسته
- یا اجاره یه مک مجازی (MacStadium و مشابه) فقط برای مرحله بیلد

کد نوشتن و توسعه (با Claude Code) روی هر سیستمی (ویندوز/لینوکس/مک) قابل انجامه؛ فقط مرحله بیلد نهایی iOS نیاز به محیط مک داره.

---

## بخش ۵: فازهای پیاده‌سازی (به‌روزشده)

### فاز ۰ — راه‌اندازی معماری (۳-۴ روز)
- ساخت پروژه Flutter با ساختار لایه‌بندی‌شده بالا
- تنظیم Riverpod
- تنظیم CI/CD اولیه (GitHub Actions یا Codemagic) برای بیلد خودکار هر دو پلتفرم
- تم مشترک (رنگ، فونت، دارک/لایت)
- Firebase Analytics + Crashlytics

### فاز ۱ — فیچرهای هسته‌ای MVP (۱.۵-۲ هفته)
- Merge / Split / Compress PDF (پردازش لوکال، در Isolate)
- Image to PDF / PDF to Image
- لیست فایل‌های ساخته‌شده در اپ (History) با lazy loading
- زیرساخت تبلیغات (Banner + Interstitial)
- تست روی شبیه‌ساز iOS و Android هر دو

### فاز ۲ — اتصال Drive + تمایز (۲-۳ هفته)
- ورود با گوگل + اتصال Drive API (Scope: drive.file)
- آپلود/دانلود فایل با Drive
- اسکنر سند با دوربین (edge detection)
- امضای دیجیتال
- ویرایش صفحات (چرخش، حذف، ترتیب)
- رمزگذاری PDF با پسورد
- Watermark

### فاز ۳ — مونتایزیشن Pro (۱-۲ هفته)
- In-App Purchase / Subscription (پکیج `in_app_purchase` یا RevenueCat برای مدیریت راحت‌تر هر دو استور)
- OCR (فقط Pro)
- Auto-backup به Drive (فقط Pro)
- Paywall + Trial ۳ روزه

### فاز ۴ — انتشار و بهینه‌سازی مستمر
- بیلد نهایی هر دو استور از طریق CI/CD
- ASO (بهینه‌سازی صفحه استور)
- تست عملکرد روی گوشی‌های ضعیف‌تر (برای اطمینان از سرعت)
- مانیتور حجم اپ بعد از هر فیچر جدید (جلوگیری از bloat)

---

## جمع‌بندی معماری

| نیاز تو | راه‌حل |
|---|---|
| هر دو پلتفرم، یک کدبیس | Flutter |
| سرعت بالا | پردازش لوکال + Isolate + کامپایل native |
| حجم کم | App Bundle/Thinning + lazy loading پکیج‌های سنگین |
| اتصال Drive | Drive API v3 با Scope محدود drive.file |
| بیلد یک‌جا برای هر دو استور | یک کدبیس Flutter + CI/CD (Codemagic/GitHub Actions) برای بیلد iOS بدون نیاز به مک فیزیکی |
