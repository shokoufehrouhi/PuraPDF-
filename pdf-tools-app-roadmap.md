# مستند فیچرها و رودمپ ساخت اپ PDF Tools

## معرفی کلی
اپ موبایل (Flutter) برای مدیریت فایل PDF: ادغام، تجزیه، فشرده‌سازی، تبدیل فرمت، امضا و اسکن. مدل درآمد: تبلیغات (AdMob: Banner + Interstitial + Rewarded) + اشتراک Pro بدون تبلیغ و با فیچرهای پیشرفته.

---

## فاز ۰ — راه‌اندازی پروژه (۲-۳ روز)
- ساخت پروژه Flutter، تنظیم ساختار پوشه‌ها (features-based architecture)
- نصب پکیج‌های پایه: `pdf`, `syncfusion_flutter_pdf` یا `pdfrx`, `file_picker`, `path_provider`, `share_plus`
- تنظیم تم (رنگ، فونت، دارک/لایت مود)
- راه‌اندازی ناوبری (Bottom Navigation یا Drawer)
- اتصال Firebase (Analytics + Crashlytics) — برای مانیتور کردن رفتار کاربر از روز اول

**خروجی فاز:** اسکلت اپ قابل اجرا با ناوبری بین صفحات خالی

---

## فاز ۱ — فیچرهای هسته‌ای (MVP) — ۱ تا ۲ هفته
اینا حداقل چیزیه که برای انتشار اول لازمه.

### ۱. ادغام PDF (Merge)
- انتخاب چند فایل PDF از حافظه گوشی
- امکان جابجایی ترتیب فایل‌ها (drag & drop)
- پیش‌نمایش صفحه اول هر فایل
- خروجی: فایل PDF واحد + دکمه Share/Save

### ۲. تقسیم PDF (Split)
- انتخاب فایل PDF
- انتخاب حالت: تقسیم بر اساس رنج صفحه (مثلاً ۱-۵)، یا هر صفحه جدا
- خروجی: چند فایل جدا یا یک ZIP

### ۳. فشرده‌سازی PDF (Compress)
- سه سطح فشرده‌سازی: کم، متوسط، زیاد (کیفیت تصاویر داخل PDF رو کم می‌کنه)
- نمایش حجم قبل/بعد
- پردازش لوکال (روی دستگاه) — بدون نیاز به سرور در فاز اول

### ۴. تبدیل فرمت (پایه)
- Image to PDF (چند عکس → یک PDF)
- PDF to Image (هر صفحه → PNG/JPG)

### ۵. مدیریت فایل داخلی اپ
- لیست تمام فایل‌های ساخته‌شده در اپ (History)
- امکان حذف، rename، share مجدد
- جستجو در لیست فایل‌ها

### ۶. زیرساخت تبلیغات
- Banner Ad در صفحه اصلی
- Interstitial Ad بعد از هر عملیات (مثلاً هر ۲-۳ بار استفاده، نه هر بار — برای جلوگیری از آزار کاربر)
- ثبت اپ در AdMob و تست با Test Ad ID

**خروجی فاز:** اپ قابل انتشار در استور با فیچرهای پایه‌ای که رقبا هم دارن

---

## فاز ۲ — تمایز و ارزش افزوده — ۲ تا ۳ هفته
فیچرهایی که اپ رو از رقبا متمایز می‌کنه و باعث نصب/نگه‌داشت کاربر می‌شه.

### ۷. اسکنر سند با دوربین
- گرفتن عکس از سند با دوربین
- تشخیص خودکار لبه سند (edge detection) و کراپ
- فیلتر: سیاه‌سفید، رنگی، افزایش کنتراست
- ذخیره چند صفحه در یک PDF

### ۸. امضای دیجیتال روی PDF
- کشیدن امضا با انگشت (signature pad)
- قرار دادن امضا روی هر جای صفحه PDF
- ذخیره امضا برای استفاده مجدد

### ۹. ویرایش صفحات
- چرخش صفحه (Rotate)
- حذف صفحه از PDF
- تغییر ترتیب صفحات

### ۱۰. رمزگذاری و حفاظت
- قفل کردن PDF با پسورد
- حذف پسورد از PDF قفل‌شده (با داشتن پسورد فعلی)

### ۱۱. Watermark
- اضافه کردن متن یا لوگو به عنوان واترمارک روی همه صفحات

### ۱۲. سیستم Rewarded Ad
- برای فیچرهای سنگین‌تر (مثلاً OCR یا فشرده‌سازی زیاد) کاربر می‌تونه با دیدن ویدیو تبلیغاتی، یک بار رایگان استفاده کنه

**خروجی فاز:** اپی که نسبت به اپ‌های ساده بازار تمایز واضح داره

---

## فاز ۳ — مونتایزیشن پیشرفته و Pro — ۱ تا ۲ هفته
- طراحی پلن اشتراک (ماهانه/سالانه) با RevenueCat یا In-App Purchase مستقیم
- فیچرهای انحصاری Pro:
  - بدون تبلیغ
  - OCR (تبدیل عکس/اسکن به متن قابل کپی)
  - فشرده‌سازی نامحدود با کیفیت بالا
  - بدون محدودیت تعداد فایل در روز
- صفحه Paywall با طراحی جذاب و مقایسه Free vs Pro
- Trial رایگان ۳ روزه برای افزایش نرخ تبدیل

**خروجی فاز:** منبع درآمد دوگانه (تبلیغات برای کاربر رایگان + اشتراک برای کاربر جدی)

---

## فاز ۴ — رشد و بهینه‌سازی — مستمر
- A/B تست جای‌گذاری تبلیغات (کجا بیشترین درآمد رو بدون افت نصب می‌ده)
- بهینه‌سازی ASO (عنوان، کیورد، اسکرین‌شات در استور)
- اضافه کردن زبان‌های بیشتر (لوکالایز)
- اضافه کردن ویجت (Home Screen Widget) برای دسترسی سریع
- بررسی بازخورد کاربران و رفع باگ‌ها
- تحلیل داده‌های Firebase Analytics برای فهمیدن پرکاربردترین فیچر

---

## جمع‌بندی زمانی

| فاز | مدت تقریبی | هدف اصلی |
|---|---|---|
| فاز ۰ | ۲-۳ روز | راه‌اندازی پروژه |
| فاز ۱ | ۱-۲ هفته | MVP قابل انتشار |
| فاز ۲ | ۲-۳ هفته | تمایز از رقبا |
| فاز ۳ | ۱-۲ هفته | مونتایزیشن Pro |
| فاز ۴ | مستمر | رشد و بهینه‌سازی |

**نکته مهم:** بعد از فاز ۱ حتماً منتشر کن و بازخورد بگیر، بعد فازهای بعدی رو بر اساس بازخورد واقعی اولویت‌بندی کن — نه فرضی.
